---
import AboutNav from "@components/AboutNav.astro";
import Layout from "@layouts/Layout.astro";
import { siteConfig, usesHardware, usesSoftware } from "../../data";
---

<Layout
  title="Uses / About"
  description={`Learn more about the tools and software ${siteConfig.name} uses on a regular basis.`}
>
  <article class="container">
    <header class="page-hero animate-in">
      <AboutNav />
      <span class="page-hero__eyebrow">Information</span>
      <h1 class="page-hero__title">Uses</h1>
      <p class="page-hero__subtitle">
        Inspired by <a href="https://wesbos.com/" rel="nofollow" target="_blank"
          >Wes Bos</a
        > and <a href="https://uses.tech/" rel="nofollow" target="_blank"
          >uses.tech</a
        >, here is a list of some of the tech related things I use on a regular
        basis.
      </p>
    </header>

    <div class="about-body prose animate-in animate-in--delay-1">
      <p>
        These lists could go on for a very long time, but I'll do my best to
        keep them succinct.
      </p>
      <h2 id="software">Software</h2>
      <ul class="uses-list">
        {
          usesSoftware.map((item) => (
            <li class="uses-list__item">
              <a href={item.href} rel="nofollow" target="_blank">
                <strong>{item.title}</strong>
                <span>{item.description}</span>
              </a>
            </li>
          ))
        }
      </ul>
      <h2 id="hardware">Hardware</h2>
      <ul class="uses-list">
        {
          usesHardware.map((item) => (
            <li class="uses-list__item">
              <strong>{item.title}</strong>
              <span>{item.description}</span>
            </li>
          ))
        }
      </ul>
      <p>
        There's a few things I'd like to try in the future; including a standing
        desk <span class="subtle">(to help with posture)</span> and improved lighting.
      </p>
      <footer>
        <hr />
        <p><a href="/about">← Back to About</a></p>
      </footer>
    </div>
  </article>
</Layout>

<style>
  .uses-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    list-style: none;
    margin: var(--space-4) 0 var(--space-6);
    padding: 0;
  }

  .uses-list__item {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: var(--space-4);
  }

  .uses-list__item a {
    text-decoration: none;
  }

  @media (hover: hover) {
    .uses-list__item:hover {
      border-color: var(--color-accent);
    }
  }

  .uses-list__item a strong {
    text-decoration: underline;
  }

  .uses-list__item a span {
    color: var(--color-text-muted);
  }

  .uses-list__item strong {
    display: block;
    font-size: var(--text-lg);
    margin-block-end: var(--space-1);
  }

  .uses-list__item span {
    display: inline-block;
  }
</style>
